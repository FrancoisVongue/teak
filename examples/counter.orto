// Counter using Ref[int]. Demonstrates basic mutation and ??.

fn main() -> int {
    let c = ref(0);

    // Increment three times.
    let _ = c := (deref(c) ?? 0) + 1;
    let _ = c := (deref(c) ?? 0) + 1;
    let _ = c := (deref(c) ?? 0) + 1;

    deref(c) ?? panic()   // 3
}
