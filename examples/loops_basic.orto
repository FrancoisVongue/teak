// Sum of 0..=10 via mutable counter and while loop.
// Exit code = 55.

fn main() -> int {
    let mut sum = 0;
    let mut i = 0;
    while i <= 10 {
        sum := sum + i;
        i := i + 1;
    }
    sum
}
